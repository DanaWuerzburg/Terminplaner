<section>

  <div class = "field">
        <label for="date" class="label">Date</label>
        <input type="text" disabled="disabled" value="<%= @appointment.start_at.strftime('%d. %m. %Y %H:%M') %>">
        <input type="text" disabled="disabled" value="<%= @appointment.end_at.strftime('%d. %m. %Y %H:%M') %>">
        <label for="note" class="label">Note:</label>
        <input type="text" disabled="disabled" value="<%= @appointment.note %>">

    <%  aFile = File.new("./datein/testfile", "a") %>
              <%  aFile.puts "hallo"   %>
              <%  aFile.close  %>
              <%  file = File.new("./datein/testfile2", "a") %>
              <%  aFile = File.new("./datein/testfile", "r") %>
              <%  b=aFile.gets    %>
              <%=  file.write(b)    %>
              <%  aFile.close    %>
              <%  file.close    %>



        <h4>Generate and Download Sample XML Document</h4>

              <%=link_to "Download as xml", :action => "generate_with_rexml" %>
              <%=link_to "Show as xml", :action => "show_with_rexml" %>






     <h4>Upload the Sample XML Document, Parse and Manipulate</h4>

       <% form_tag({:action => 'upload'}, :multipart => true )%>

           <label for="file"> File:   </label>
           <%= file_field("appointment","xml_file") %>
           <%= submit_tag("Parse with REXML") %>



     <%= form_tag({:action => 'upload', :id => @appointment}, :multipart => true) %>
     Fuer die zweite Implementierung noch eine Version
     Waehlen Sie eine Datei : <%= file_field("appointment", "report")  %>
     <%= submit_tag("upload au dBuch")%>


</div>

</section>